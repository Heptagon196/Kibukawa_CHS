# 第二作文本初译完成

2026-09-06，已将所有待译条目回填至 `work/cache.json`。

| 范围 | 数量 | 状态 |
| --- | ---: | --- |
| scn0–scn9 | 7,878 | 初译及独立逐章交叉审校完成 |
| 姓名与保存子场景 | 16 | 初译并核对术语、注音及选项 |
| 界面、提示、标题文本 | 94 | 初译并核对占位符与重复文案 |
| 技术/模板排除项 | 2,416 | 保留状态7及空译文字段，未混入已译计数 |

合计 7,988 条由未译状态0改为初译状态1，未设置最终完成状态。原始字符串、定位和指纹清单保持不变。两条独立姓名注音（69、813）有意为空，其他原文留白槽逐字保留。没有以复制日文或批量改状态充数。

主控统一了董事长、高管、萨纳米建设、定期乘车票、证件夹、林居／邻居等用语。交叉审校修正了楼层范围、乘电梯方向、推测语气、分支引号和跨片段语病；原文自身的日期、分钟数矛盾以及故意错称仍保留并记录。本地详细修订见 `reports/first-pass-review.json`，疑点见 `reports/translation-merge.json`。

校验覆盖：11 个批次 ID 完整，译文无日语假名、NUL 或非法控制字符；占位符和标签保留；script 单槽不超过20个 UTF-16 字符，实际最长15。源数据、排除项、第二作原文件和第一作已跟踪文件哈希检查通过。

后续已构建并安装0.2.1运行时测试版，补齐对白重排、25段固定页面、4页中文帮助和严格菜单列宽检查；8处超宽菜单标签已缩短并记录。`enabled=true`，实机效果仍由用户验证，没有启动游戏。标题等原始美术中的文字尚未全部替换。详见 [运行时交付记录](RUNTIME_DELIVERY.md)。

可读译文位于本地 `translated_texts/`；原文位于 `texts/`。这些是派生文件，正式活动译文源始终是 `work/cache.json`。目前未提交或推送 GitHub。
