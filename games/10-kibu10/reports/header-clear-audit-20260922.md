# 0.1.24 顶栏清除路径审计

原版 `CanvasEx` 只在 `FrameTask == 2`、`MainTask != 11`、非滚动、`info_struct_zenkaku_suu > 0` 时绘制 INFO。该计数的全部写入点：初始化置零；返回标题置零；清空数据置零；脚本 `INFO()` 读取新计数（可为零）；读取存档恢复计数（可为零）。`INFO(0)` 与零计数存档都保留旧 `info_struct_moji` 字符串，因此必须根据计数隐藏。章节选择 `MainTask == 12` 额外隐藏顶栏，因为原版该入口没有清空计数。

0.1.23 已补计数和章节选择判断，但原文未命中译文表时会先放行原生 painter。本版将状态判断置于译文表查询之前，确保已翻译和未翻译 INFO 遵守同一可见性条件。正文中仍未翻译的有效 INFO 保持原生绘制。

离线测试覆盖清零、标题、章节选择、未翻译回退、正常顶栏；整包 verify 与六组初始化确认帧回放通过。未操作游戏窗口，实机效果由用户验证。

包：`out/CHS_20260922_005045_891450300/Kibu10_CHS.zip`

SHA256：`091fcd9cabb705ec79b54283bb79577b3b060db107ee43fc29cf46cb26ca1682`

安装记录：`installations/20260922_005059`；102 个安装文件哈希与验证包一致。
