# 0.1.23 顶栏残留与章节底栏右对齐

原作 `PaintMain_info` 仅在 `info_struct_zenkaku_suu > 0` 时画顶栏。返回标题和清空存档的原版路径会将此计数设为 0，但保留 `info_struct_moji` 字符串。补丁此前只凭残留字符串继续绘制，故选页上叠着上一段的姓名、日期和地点。替代绘制恢复原计数门槛，并在 `MainTask == 12` 章节选择时明确隐藏 INFO。

同包包含 0.1.22 的章节底栏按显示字形宽度右对齐修复。测试覆盖信息计数清零、标题状态、章节选择状态，以及正常剧情仍能显示 INFO；底栏两条不同日期共用右边界。整包 verify、真实 40 BIN、控制码和初始化确认六组原始指令回放通过。未操作游戏窗口，实机效果仍由用户验证。

包：`out/CHS_20260922_004726_699617400/Kibu10_CHS.zip`

SHA256：`35cf5d58b7bbc66c3f6a9ebc795c0206f9e6391c1ad24609682c423b404a0170`

安装记录：`installations/20260922_004739`；102 个文件逐一核对与验证包相同。
