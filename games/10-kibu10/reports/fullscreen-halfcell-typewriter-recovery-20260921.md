# 全屏半格间距与逐字显示死循环恢复

- 主插件版本：0.1.14
- 全屏混排：全屏说明页沿用原作 12px 网格，中文为 12px，英文/数字为 6px，中英文边界空格固定为对称的半格 6px。“键盘　Ｅ　键”在全屏网格中总宽 54px，E 起点为 30px，后一中文起点为 42px。
- 排版一致性：全屏测宽、动态规划重排、居中和逐字绘制使用同一套 12/6px 坐标；普通对话框继续使用既定 17/9px 正文步进与 4px 混排间距。
- 卡死根因：原始 `CanvasEx.Game_adv` 的逐字显示 `catch` 为 `pop; br.s` 回跳自身，任何行列越界都会让 Unity 主线程永久循环。离线 ABI 测试已对 SHA256 锁定程序集的第 778–782 条 IL 逐条确认。
- 恢复行为：只替换这一条永久循环。异常时记录出错前的请求/完成行列，把文本推进到最近的原生 `0x2E/0x3A/0x3B/0x2B` 终止控制，设置完整重绘；若缓冲本身损坏则结束当前文字任务，保证主线程返回。
- 初始化数据：继续保留 `file/append:9454` 的原生四行结构与末行 `0x2E` 点击事件，同时由上述恢复路径保证任何残余游标异常不会锁死游戏。
- 校验：完整 `verify --game kibu10` 通过；真实 IL 形状、恢复状态、全屏半格坐标、40 个 BIN、8,907 个显示块、943 个字符串及全部 UI/图片/历史测试通过。
- 正式包：`out/CHS_20260921_005412_351510500/Kibu10_CHS.zip`
- SHA256：`e25d42747a6e3e6d819327022ef9fbf89847d8120547aadc09d6ec0e66dbe276`
- 安装记录：`installations/20260921_005425`
- 安装后主插件 DLL SHA256：`e69a20dc4f5e4b24bdf12aac0bff9a92ab3d6f42918aeb99267c826a03937d03`
- 安装结果：102 个补丁文件逐项哈希核对通过；原始游戏及存档文件未改变；未启动或操作游戏窗口。
