# 0.1.22 章节底栏右边距

用户截图中「动机［７月９日］」与「古怪的侦探［７月１０日］」的可见右边距不一致。原版 `PaintSentaku` 以 `242 - 6 * Strlen(SubTitle)` 放置字幕，选用 12px 字体。汉化小字库将全角数字显示为半宽 6px 字形，但底栏 `Strlen` 仍把每个全角数字算作 12px。第一句含两个全角数字，第二句含三个，故第二句多退一个 6px 字格；截图按 6.75 倍游戏像素显示时约多 40px。

仅在章节底栏的测宽作用域，将全角英数按实际显示字形计为半宽。两句分别计 14 与 21 个 6px 单元；渲染可见宽度也为 84 与 126px，因此共用相同右边界。普通菜单的 `Strlen` 保持原语义。新增测试覆盖两句、菜单作用域恢复、底栏原有垂直居中；整包 verify 通过，包括真实 40 BIN、控制码绑定、菜单、历史、图片及初始化确认回放。

游戏进程仍在运行，安装器无法读取使用中的 BepInEx 缓存。没有结束进程或操作游戏窗口；待用户退出游戏后安装。

包：`out/CHS_20260922_004325_405658600/Kibu10_CHS.zip`

SHA256：`8335b6d74e3c1e78ff81949d051aa98b5b962dfafe0f57f34c0093f72960c4af`
