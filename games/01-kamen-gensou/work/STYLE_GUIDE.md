# 《假面幻想杀人事件》简体中文翻译风格（已确认）

用户已确认 work/sample_opening.json 的开场样译及自然口语风格，授权并行翻译全文。保持忠实、自然的推理游戏文风，保留人物幽默、语气与所有线索，不删减、不提前揭示身份。

- 使用简体中文。日文汉字姓名转简体，严格按照 glossary.locked.json。根据用户最新要求，人物姓名及绰号只显示中文写法，删除括号内片假名读音（含跨碎片注音）；术语表中的读音仅供翻译参考，不显示到游戏。译文不残留日语假名；线索用中文解释，引用名称与前后证物一致；含假名的颜文字也改为中文动作。邮件中ぼく按用户指定对应在下、ひとろし音译希托罗希，保留特殊用语识别线索，禁止臆造署名来源。
- 先生：对侦探癸生川通常保留“先生”；对其他角色 さん 按语境使用“先生／小姐”等，自然处理敬语，不机械保留日语敬称。
- 对话使用中文引号“”。原文常仅有 ｢ 开头没有闭引号，按完整发言结尾补闭引号；不能每一碎片都加引号。内层作品/名称用《》或『』，统一同一语境。
- 使用中文标点。…… 与原文长停顿按情绪合理使用；避免为所有省略号机械翻倍，留出20字空间。
- 最重要：原文每条是显示/颜色控制指令中的短碎片，可能把一个日语词拆成2条。先连接上下文理解整句，再在同样索引数中自然分配中文；一般每条必须非空，不丢失信息，不用空格/无意义标点敷衍占位。已审阅的纯姓名注音片段按 pipeline.py 的精确白名单译为空字符串，保留原槽位和控制指令，不得以 null 替代。
- 中文必须调整语序，不能让脚本碎片限制中文句法。先通读完整发言及前后文，再按自然中文重组主语、修饰语、问句及后置补充，最后分配回原槽位；保留高亮对象和原等待时机。示例：｢…なんですか？／その手は｡ → “……您伸手想干什么？”；不能逐块照搬为“……您想干吗？那只手。”。字宽与行数检查不能代替人工语言审校。
- index完全不变，每条只输出一个字符串，不插入换行或制表符，每条保留20个UTF-16字符的存储上限。不要把原文的显示行当作中文句子的排版边界：原文在标点后的换行可以保留；无标点处的原换行取消，中文按实际字宽自动换行。字体统一使用中文 GNU Unifont，每行最多20半格（10个汉字）且不超过20字符。
- 行数检查单位是“一次点击推进显示的对话”，以原脚本的确认等待指令为边界，不按累计留在屏幕上的历史文字计算。每次对话最多5行正文，有说话人时最多4行（姓名另占一行）。原等待确认后仅重置本次未读行数预算，不清空正文。保持原向下续排与滚动，只能滚走已获确认阅读机会的历史，不能滚掉本次尚未确认的文字。只有本次未读内容自身超过容量才追加确认等待，随后继续滚动。清屏仅保留原脚本的行为，不能新增点击后清屏。闭标点不能被原换行单独推到下一行；跨颜色片段使用相同排版规则。全篇构建必须通过字宽、字符缓冲、行数、等待次数及文字完整性检查。原指令/索引/存档偏移保持不变，排版由运行时插件完成。
- 如果目标短语比碎片数少，可合理让多个片段形成一句的词组；句号片段保留句号。允许在同一次点击内的相邻碎片间调整词序以符合中文，但不得把下一次点击的句首移到等待之前，或把当前短语的句尾挪到等待之后。重分片段必须同时对照原文与译文的完整点击单元；全篇合并后文字不变并不能证明点击边界正确。更改已审校单元时，重新审校后再更新 work/click_boundaries.reviewed.json，构建不得自动刷新基线。
- 保留 {0} 等格式参数、富文本标签。UI文字允许原有换行，脚本文字不允许新换行。场景选项短促明确，例如 话す→交谈、見回す→环顾、調べる→调查、捜査中断→中断调查。
- 每个选项最多四个汉字宽度（8半格，包括英文、数字和标点的实际占宽），不能只检查菜单整行。选项中的克罗什公司／克罗什株式会社统一简写为克罗什，游戏名统一写Taclimax，八个ASCII字母恰好占8半格，菜单无需另取简称。构建必须逐项校验全部菜单标签，超限则失败。
- 锁定表含少量剧情身份提示，仅供翻译理解；早期冒名角色按当时原文称呼，不要以其真实身份替换显示名。
- タクリマクス／タクマク→Taclimax（用户指定，正文、简称及菜单统一）；アルファ・ジェネシス→阿尔法创世纪；オバキュー→尾Q。涉及构词、谐音、英文词义的线索，日语部分改为中文解释，英语词义线索可保留必要英语，不机械替换导致线索失效。
- 表外专名：汉字名依简体字形保留、假名专名须提出中文音译并由主任务结合上下文复核；原文保留在审校记录，不显示到译文。
- 已确认样译（scn0的14～53）不重译，直接采用样译文件对应文本。

产出：work/parallel/output/<任务>.json，内容为 [{"text_index":编号,"translated_text":"译文"},...]，顺序/条数与输入一致；另写 <任务>.review.json 记录需复核的新词/线索/疑点，无则 []。不要写 cache.json、manifest.json、glossary 或其他任务输出。

- 原文谐音无法直接用中文表现时，忠实翻译角色话语，必要解释放在角色引号之外，以（译注：……）标注；不得让角色自己讲解翻译，也不得为凑中文谐音大幅改写原意。音成按用户指定译为林居，全名林居孝一，与邻居在中文中同音，因此该处不加译注。

- 用户指定：本名狭川美妮，网名米妮；保持两者身份与冒名剧情区别，删除该姓名的日语同音译注和mini读音标注。

- 网游聊天的发言者切换是结构性换行，不受上一条消息是否有句末标点影响。不得将姓名与上一条消息连写；仅同一条消息内部的原始折行可合并。

- Snowman是怪物专名，系统名译Snowman系统；相关邮件与留言保留Snowman。普通雪人、雪人服装和周边仍译雪人，禁止把专名和形状名合并。
