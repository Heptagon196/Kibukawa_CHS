# G-MODE v1 适配器

换行策略（2026-09-06）：两作共用的 DialogueReflow 按行宽与缓冲容量直接换行，不做行首/行尾禁则，也不为后续标点回搬已显示的字符。原有语句边界判定、颜色、逐字显示和分页保护保留。

当前仅由 kibu1 原程序集和指令回放验证。负责 CanvasEx 的文本缓冲、滚动、菜单状态与字体绘制。容量常量是已验证版本的约束，不能在未检查后续引擎时沿用。

聊天边界、固定名单范围、帮助页、字符串地址与 Plugin 挂钩入口暂留第一作；第二作完成结构对照后再抽取重复部分。

第二作已完成静态结构对照：文本相关方法、SJIS 表与指令定义一致，画面高度和部分坐标存在差异；详见 [第二作接入记录](../../../games/02-kairou/COMPATIBILITY.md)。探针仅编译公共字体、菜单和译文包组件，尚未接入第二作文本挂钩或 DialogueReflow，不能据此宣称第二作实机兼容。

公共结构换行（2026-09-06）：DialogueReflow跟踪实际执行的原文行、颜色及77/78边界，保留整行非默认同色文字后、恢复默认色并开始引号对白的换行。无游戏地址、姓名或颜色编号特判；正文彩色关键词不强制换行。三作完整构建回归通过；截图及正反用例见第三作DialogueReflowTests。
